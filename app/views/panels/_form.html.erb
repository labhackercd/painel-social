<%= simple_form_for @panel, 
  wrapper_mappings: {
    check_boxes: :vertical_radio_and_checkboxes,
    radio_buttons: :vertical_radio_and_checkboxes,
    file: :vertical_file_input,
    boolean: :vertical_boolean
  } do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, placeholder: 'Título do Painel' %>

  <%= f.input :query, placeholder: 'Termos de busca, ex.: #camara, Câmara dos Deputados, @camara' %>

  <%= f.button :submit %> ou <%= link_to 'Cancelar', panels_path %>
<% end %>
